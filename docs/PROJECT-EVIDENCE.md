# Real project evidence

Add screenshots from OrderFlow and KRUNG here, in separate `orderflow/` and `krung/` subfolders. Do not add recreated interfaces or stock images as project evidence.

Configure each project's `evidence.image`, `alt`, `caption`, `description` and `process` in `src/data/portfolio.ts`. Paths are relative to `public`, for example `projects/orderflow/checkout.png`; Vite resolves them beneath the deployed `/portfolio/` base.

Useful evidence still missing:

- OrderFlow: customer checkout and the corresponding seller order view; a real bug or earlier version with an explanation of the change.
- KRUNG: a storyline showing sources and related events; an actual layout iteration or code excerpt; the exact technology stack.

Keep dates, source links, screenshots and personal reflections accurate. Empty configuration keeps the visible placeholders.
