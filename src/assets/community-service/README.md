Place jpg, jpeg, png, webp or avif photographs here, then rebuild.
Images are discovered automatically in filename order. Lowercase extensions are recommended; uppercase variants are supported.
Set an entry's image field in src/data/portfolio.ts to a filename to bind it explicitly.
Unassigned images automatically fill empty frames; additional images produce extra frames.
Add accurate captions, dates, organisations, roles and impact to the same data file, and set placeholder to false only when those details are confirmed.
Images without confirmed captions remain labelled as details pending. Use descriptive filenames and appropriately sized images.
